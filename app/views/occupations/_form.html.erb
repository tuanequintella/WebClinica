<%= simple_form_for(occupation, :html => { :class => 'form-horizontal' }) do |f| %>
  <fieldset>
    <%= f.input :name %>
    <div class="form-actions">
      <%= f.button :submit, "Salvar" %>
      <%= link_to "Cancelar", occupations_path, :class => :btn %>
    </div>
  </fieldset>
<% end %>
