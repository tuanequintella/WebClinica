<%= simple_form_for(health_insurance, :html => { :class => 'form-horizontal' }) do |f| %>
  <fieldset>
    <%= f.input :name %>
    <div class="form-actions">
      <%= f.button :submit, "Salvar" %>
      <%= link_to "Cancelar", health_insurances_path, :class => :btn %>
    </div>
  </fieldset>
<% end %>
