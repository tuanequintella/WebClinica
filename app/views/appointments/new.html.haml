=simple_nested_form_for(@appointment, :html => { :class => 'form-horizontal' }) do |f|
  %fieldset
    -f.input :agenda, :hidden => true
    =f.input :scheduled_at

    =f.input :record, :collection => Pacient.records_list, :include_blank=> "Novo paciente"
    (infos do paciente ou form de novo aqui)

